import { LockIcon } from "@chakra-ui/icons";
import { Box, Button, Container, Modal, ModalBody, ModalCloseButton, ModalContent, ModalOverlay, Text } from "@chakra-ui/react";
import React, { useState } from "react";
export const Kirish = () => {
  return (
      <>

      </>    
  );
};
